image sdcard.img {
  hdimage {
    partition-table-type = "dos"
  }

  partition boot {
    partition-type = 0xC
    bootable = "true"
    image = "boot.vfat"
    size = 256M
  }

  partition rootfs {
    partition-type = 0x83
    image = "rootfs.ext4"
    size = 8000M
  }
}
